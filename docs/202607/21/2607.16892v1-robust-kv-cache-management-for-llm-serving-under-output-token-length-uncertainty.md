---
title: Robust KV Cache Management for LLM Serving under Output Token Length Uncertainty
title_zh: 面向输出令牌长度不确定性的LLM服务鲁棒KV缓存管理
authors: "Jiaming Cheng, Duong The Do, Duong Tung Nguyen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16892v1"
tags: ["query:llm-kv-cache"]
score: 9.0
evidence: 鲁棒的KV缓存管理，降低内存占用和抢占风险
tldr: "LLM服务中KV缓存需提前预留但输出长度未知，导致预分配不足引发抢占或过度浪费。本文提出Wasserstein分布鲁棒优化框架，联合优化GPU并行配置、每类请求缓存预留、请求路由及前缀缓存，自动调整预留分位数。在BurstGPT等生产负载上相比固定分位数基线降低最高56%成本，同时保持P99延迟和SLO违例率。该方法无需手动调参，适应不同抢占与内存成本场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16892v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 1121, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 920, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 709, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16892v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 921, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 842, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1136, \"height\": 227, \"label\": \"Table\"}]"
motivation: 输出令牌长度不确定性导致KV缓存预留难以权衡：预留不足引发抢占和重计算，过度则浪费内存降低吞吐量。
method: 建立Wasserstein分布鲁棒优化（DRO）模型，联合优化GPU并行、缓存预留、路由和前缀缓存，设计块坐标下降算法求解混合整数问题。
result: "在BurstGPT、Azure、ShareGPT轨迹上，相比固定分位数基线降低最高56%成本，P99延迟和SLO违例率保持竞争力。"
conclusion: 自动适应不同预占与内存成本的分位数调整策略，有效提升LLM服务内存效率与吞吐量，无需人工调参。
---

## 摘要
KV缓存内存是现代部署在GPU集群上的LLM服务系统中的主要瓶颈。一个基本挑战是，KV缓存必须在请求到达时预留，而输出令牌长度直到生成完成才可知。预留不足会触发抢占——强制终止并重新计算请求，带来巨大开销——而预留过多则浪费内存并降低吞吐量。这就在内存效率与抢占风险之间形成了一个核心权衡。我们提出了一种用于LLM服务的鲁棒KV缓存管理框架，该框架联合优化了GPU并行配置、每请求类别的KV缓存预留、跨异构服务组的请求路由以及共享提示的前缀缓存。该框架考虑了延迟SLO约束，并捕捉了内存分配、吞吐量和排队延迟之间的交互。为了解决输出令牌长度不确定性和工作负载分布偏移，我们开发了一种Wasserstein分布鲁棒优化（DRO）公式，以及针对由此产生的混合整数问题的可扩展块坐标下降算法。我们的分析揭示了一个关键的分位数结构，该结构能够自动适应不同的抢占和内存成本机制，无需手动调整。在包括BurstGPT、Azure和ShareGPT轨迹在内的生产级LLM工作负载上的轨迹驱动评估表明，与固定分位数预留基线相比，成本降低高达56%，同时在不同运行机制下保持了具有竞争力的P99延迟、有效吞吐量和SLO违规率。

## Abstract
KV cache memory is a primary bottleneck in modern LLM serving systems deployed on GPU clusters. A fundamental challenge is that the KV cache must be reserved upon request arrival, while the output token length remains unknown until generation completes. Under-reservation triggers preemption -- forcing termination and recomputation of requests and incurring significant overhead -- whereas over-reservation wastes memory and reduces throughput. This creates a central trade-off between memory efficiency and preemption risk. We present a robust KV cache management framework for LLM serving that jointly optimizes GPU parallelism configuration, KV cache reservation per request class, request routing across heterogeneous serving groups, and prefix caching for shared prompts. The framework incorporates latency SLO constraints and captures the interaction between memory allocation, throughput, and queueing delay. To address output token length uncertainty and workload distribution shift, we develop a Wasserstein distributionally robust optimization (DRO) formulation together with a scalable block coordinate descent algorithm for the resulting mixed-integer problem. Our analysis reveals a critical fractile structure that automatically adapts reservation quantiles to different preemption and memory cost regimes without manual tuning. Trace-driven evaluation on production LLM workloads, including BurstGPT, Azure, and ShareGPT traces, demonstrates up to 56\% lower cost than fixed-quantile reservation baselines while maintaining competitive P99 latency, goodput, and SLO violation rates across diverse operating regimes.

---

## 论文详细总结（自动生成）

# 详细论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在LLM服务系统中，KV缓存必须在请求到达时预先分配，但输出令牌长度直到生成完成才可知。预留不足会触发抢占（preemption），导致已生成的令牌被丢弃并重新计算，带来巨大开销；预留过多则浪费内存，降低有效并发数，进而减少吞吐量。这构成了内存效率与抢占风险之间的根本权衡。
- **研究动机**：现有工作主要关注运行时机制（如PagedAttention、连续批处理、预填充/解码分离），但将预留和路由决策留给操作员手动设定固定分位数（如P90、P99等）。这些固定分位数无法适应工作负载分布漂移和不同成本结构，导致在部分场景下成本过高。
- **整体含义**：本文首次将分布鲁棒优化（DRO）引入KV缓存管理，提出联合优化GPU并行配置、每类请求的KV缓存预留、请求路由和前缀缓存的统一框架，以在输出长度不确定性下实现鲁棒且自适应的内存管理。

## 2. 论文提出的方法论

### 核心思想
- 将KV缓存预留问题建模为Wasserstein分布鲁棒优化（DRO）问题，使用一个以经验分布为中心、半径为ε的Wasserstein球构造模糊集，保护系统免受分布偏移影响。
- 揭示最优预留遵循**关键分位数结构（critical fractile structure）**：最佳缓冲量η* = F^{-1}(ρ/(ρ+1))，其中ρ = cp/cw 是抢占成本与浪费成本之比。该分位数自动适应不同成本机制，无需手动调参。

### 关键技术细节
- **系统模型**：考虑GPU集群（J个GPU）、多个候选并行配置（TP/PP）、多个请求类（不同输出长度分布）、延迟SLO约束。决策变量包括：配置部署数量z_k、每类预留量r_i、路由比例π_{i,k}、前缀缓存标志δ_{i,k}。
- **成本结构**：单体请求的随机成本 = cp*(溢出)^+ + cw*(浪费)^+，再加上GPU使用成本、SLO违规惩罚、拒绝惩罚。
- **内存容量约束**：通过Little定律将预留与并发数关联，加入安全因子(1+κ)应对瞬时波动。
- **DRO公式**：使用1-Wasserstein距离构造模糊集，通过Kantorovich对偶转化为有限维线性规划。最终问题为混合整数双线性规划（MIBLP）。
- **算法（BCD-DRO）**：块坐标下降算法，将问题分解为三个块交替优化：连续变量（路由、预留、对偶变量）→ 前缀缓存闭式更新 → 整数配置枚举。可扩展至大规模实例。

### 公式或算法流程（文字说明）
1. 初始化配置和预留。
2. 循环直到收敛：
   - 块1：交替求解两个线性规划（先固定γ,θ,r优化π，再固定π优化r,γ,θ）。
   - 块2：根据路由后的负载决定是否缓存前缀（δ=1若单元负载>1）。
   - 块3：枚举可行配置集Z，选择使目标最优的z。
   - 检查是否稳定。
3. 返回最优解。

## 3. 实验设计

### 数据集
- **BurstGPT**：1.4M请求，61天，Azure OpenAI服务输出长度（均值125，P90=276，P99=1586 tokens）。
- **Azure LLM 2024**：44M请求，7天，两类（Code: 均值23, P90=49; Conversation: 均值117, P90=398）。
- **ShareGPT**：368K对话，均值265，P90=486，P99=768 tokens。

### 场景
- 按成本比率ρ划分六种应用场景：Batch (ρ=2)、Async API (ρ=5)、User-facing (ρ=10)、Latency-sensitive (ρ=20)、Critical (ρ=50)、Real-time (ρ=100)。

### 对比方法（baselines）
- **固定分位数**：Mean, P90, P95, P99, Max。
- **长度预测**：LP+1σ, LP+2σ（基于均值+标准差的安全裕度）。
- **SAA**（样本平均近似，ε=0）作为DRO的消融对照。

### 其他设置
- 模拟J=8 A100-80GB GPU，模型70B参数，四种并行配置（TP=2/4，PP=1/2）。
- 默认cp/cw=10，c_slo/cw=5，c_rej/cw=5000，κ=0.2。
- 评估指标：总成本、P99延迟、有效吞吐量（goodput）、SLO违规率。

## 4. 资源与算力

- 论文明确使用**8块A100-80GB GPU**进行仿真（J=8），模拟70B参数模型。
- **未明确说明**训练/求解算法的具体GPU时长或总计算量。文中提到BCD-DRO算法在最大规模实例（15类×12配置×2000样本）上运行时间约257.7秒，但未说明使用的硬件配置（单CPU还是GPU求解LP）。属于优化求解而非深度学习训练，算力需求相对较低。

## 5. 实验数量与充分性

- **实验组数充足**：包括：
  - 跨六个成本比率（ρ）对比多种baseline（表II、表IV）。
  - 算法可扩展性测试（表III：从3类×4配置到15类×12配置）。
  - 敏感性分析：不同到达率缩放（图3a）、Wasserstein半径ε（图3b）、成本权重c_slo/c_rej（图4）。
  - 分布偏移鲁棒性（图5）：静态vs滚动重优化；DRO优势随偏移增大而增加。
  - 消融实验（表V）：分别去掉路由优化、DRO（ε=0）、前缀缓存，测试在2.5倍偏移下的性能。
  - 长度预测基线对比（表IV）。
  - DRO分位数与理论公式对比（图2b）。
- **充分性评估**：
  - 数据集覆盖聊天、代码、对话等真实生产负载，具有代表性。
  - 对比方法涵盖了实际系统中常用的启发式策略。
  - 消融实验验证了每个组件的贡献。
  - 敏感性分析展示了参数变化下的稳定性。
  - 实验设计较为全面，但缺少与更复杂的预测模型（如基于prompt的回归预测）的对比，仅使用了LP+kσ。

## 6. 论文的主要结论与发现

1. **关键分位数结构是理论核心**：最优预留分位数完全由成本比ρ决定（q*=ρ/(ρ+1)），实验验证DRO的分位数完美跟踪该公式（图2b）。
2. **固定分位数仅在窄区间最优**：P90仅在ρ≈10附近最优，P95在ρ≈20附近，而DRO在所有ρ下均保持低成本（图2a、表II）。
3. **DRO比SAA更鲁棒**：在分布偏移下，DRO的P99延迟增长更慢，SAA在3倍偏移时P99达18.0秒，DRO仅9.1秒（图6）。
4. **算法可扩展**：BCD-DRO比直接求解MIBLP快数十倍，可在5-10分钟内重优化大规模实例（15类×12配置×2000样本）。
5. **滚动重优化适应漂移**：静态策略在分布偏移下成本急剧上升，而滚动时域策略保持稳定（图5a）。
6. **路由是可行性约束**：均匀路由会导致无效分配，显著恶化性能（消融实验成本从13k升至93k）。
7. **前缀缓存主要降低排队延迟**：在消融实验中成本不变但P99从8.1s升至9.95s。

## 7. 优点

- **方法论创新**：首次将分布鲁棒优化系统化地应用于LLM服务KV缓存管理，不仅能处理输出长度不确定性，还能对付分布漂移。
- **理论深刻**：推导出关键分位数结构，使得最优策略具有可解释性和自动适应性，无需手动调参。
- **联合优化**：同时考虑并行配置、预留、路由、前缀缓存，捕捉了它们之间的相互影响（如通过Little定律关联预留与并发数）。
- **算法实用**：BCD-DRO能够处理大规模混合整数问题，收敛快，适合在线重优化部署。
- **实验全面且公平**：覆盖多个生产数据集，对比多种baseline，消融实验清晰，敏感性分析充分。
- **鲁棒性优势**：在成本比变化和分布漂移下表现稳定，而固定基线在某些区域成本爆炸。

## 8. 不足与局限

- **实验规模限制**：仅模拟8 GPU的集群，未测试更大规模（如数百GPU）的场景，实际大规模部署中并行配置枚举可能指数增长。
- **与最先进运行时系统结合**：论文强调与现有运行时（PagedAttention等）兼容，但未实际集成测试，仅用模拟器验证。
- **预测模型对比有限**：仅对比了简单的LP+kσ，未与基于prompt的深度学习预测模型（如[6]更复杂的版本）对比，可能低估了预测方法的上限。
- **成本参数设定主观**：cp/cw、c_slo、c_rej等成本权重的设定由实验者定义，虽做了敏感性分析，但现实中这些比值可能难以精确获取。
- **假设前提**：假设请求到达为泊松过程（M/G/1排队模型），实际流量可能存在自相关性或突发性；服务时间变异系数使用固定样本计算，未考虑动态变化。
- **前缀缓存建模简化**：仅使用0-1变量表示是否缓存前缀，未考虑前缀长度变化或缓存替换策略。
- **均匀假设**：每个请求类内的输出长度分布假定同质，但同一类内可能存在多模态分布（如不同prompt引起的极端方差）。
- **未涉及token eviction等正交方法**：论文提到可以结合H2O等，但未做联合实验，无法评估整体增益上限。

（完）
