---
title: "MosaicKV: Serving Long-Context LLM with Dynamic Two-D KV Cache Compression"
title_zh: MosaicKV：利用动态二维KV缓存压缩服务长上下文大语言模型
authors: "Sheng Qiang, Ruiwei Chen, Yinpeng Wu, Jinyu Gu, Zhichao Hua, Yubin Xia, Binyu Zang, Haibo Chen"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00760v1"
tags: ["query:llm-kv-cache"]
score: 9.0
evidence: 针对KV缓存压缩，动态压缩序列和通道两个维度
tldr: "长上下文LLM服务中KV缓存随上下文长度线性增长，耗尽GPU内存并降低吞吐。现有压缩技术仅针对序列或通道单一维度，效果有限。本文提出MosaicKV，采用动态二维压缩，根据KV向量重要性在段粒度选择策略，并利用闲置GPU/CPU资源管理压缩缓存。在H800 GPU上实现最高16倍注意力加速、4.8倍解码延迟降低、7.3倍吞吐提升，内存减少3倍，平均准确率损失仅1.76%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00760v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1822, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 875, \"height\": 931, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1571, \"height\": 795, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 869, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1833, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 844, \"height\": 519, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00760v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1841, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00760v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 937, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00760v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 194, \"label\": \"Table\"}]"
motivation: 长上下文场景下KV缓存线性增长导致GPU内存瓶颈，现有单维度压缩方法在极端长度下效果不足，需同时压缩序列与通道维度但面临精度损失问题。
method: 提出MosaicKV，通过评估KV向量内元素重要性，以缓存段为粒度动态选择二维压缩策略，并设计压缩缓存管理系统利用辅助资源加速计算。
result: "在H800 GPU上测试多个LLM，相比未压缩基线实现16倍注意力加速、4.8倍解码延迟降低、7.3倍吞吐提升，显存减少3倍，平均准确率损失仅1.76%。"
conclusion: MosaicKV通过动态二维KV缓存压缩有效解决长上下文服务的内存瓶颈，在显著提升性能的同时保持高精度，适用于超长上下文场景。
---

## 摘要
长上下文大语言模型服务现在支持数十万到数百万个token的提示，使得键值（KV）缓存成为首要的服务开销。由于缓存随上下文长度线性增长，它会耗尽GPU内存，迫使批次变小，并降低服务吞吐量。以往的KV缓存压缩技术通常只针对序列维度或只针对通道维度，这导致随着上下文窗口的扩展，效果有限。同时压缩两个维度有望实现更高的内存减少，但直接应用这两种压缩形式会导致显著的精度损失。本文介绍了MosaicKV，一种用于极长上下文服务的动态二维KV缓存压缩系统。MosaicKV利用动态二维压缩来解决精度挑战，利用KV缓存内元素重要性分布的不均匀性。MosaicKV不是全局应用一种压缩模式，而是为每个KV向量识别重要元素，并在KV缓存段的粒度上选择压缩策略。为了解决性能挑战——细粒度稀疏性和压缩管理开销可能抵消压缩带来的收益——MosaicKV引入了压缩KV缓存管理。该机制利用未充分利用的GPU和CPU资源来维护压缩KV缓存并加速注意力计算。在H800 GPU上使用多个LLM的评估表明，与未压缩基线相比，MosaicKV可实现高达16倍的注意力加速、4.8倍更低的解码延迟和7.3倍更高的吞吐量。同时，它将内存使用量减少3倍，并且在LongBench和RULER上平均精度损失仅为1.76%。

## Abstract
Long-context LLM services now sustain prompts with hundreds of thousands to millions of tokens, making the key-value (KV) cache a first-order serving cost. Because the cache grows linearly with context length, it can exhaust GPU memory, force smaller batches, and reduce serving throughput. Prior KV cache compression techniques typically target only the sequence dimension or only the channel dimension, which leaves limited headroom as context windows scale. Compressing both dimensions promises higher memory reduction, but applying the two forms of compression directly leads to significant accuracy loss.   This paper introduces MosaicKV, a dynamic two-D (dimensional) KV cache compression system for extremely long-context serving. MosaicKV uses dynamic two-D compression to address the accuracy challenge, exploiting the non-uniform importance distribution of elements within the KV cache. Instead of applying one compression pattern globally, MosaicKV identifies important elements for each KV vector and selects compression strategies at the granularity of KV cache segments. To address the performance challenge, where fine-grained sparsity and compression management overhead can offset the gains from compression, MosaicKV introduces compressed KV cache management. This mechanism uses underutilized GPU and CPU resources to maintain compressed KV caches and accelerate attention computation.   Evaluation on an H800 GPU with multiple LLMs shows that MosaicKV delivers up to 16x attention speedup, 4.8x lower decode latency, and 7.3x higher throughput than the uncompressed baseline. At the same time, it reduces memory usage by 3x and incurs only 1.76% average accuracy loss on LongBench and RULER.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：长上下文大语言模型（LLM）服务需要处理数十万至数百万token的提示，导致KV缓存大小随上下文长度线性增长，迅速耗尽GPU内存，迫使批次减小、延迟增加、吞吐下降。现有KV缓存压缩技术仅针对序列维度（保留重要token）或通道维度（保留重要通道），单一维度在极端上下文长度下压缩率受限，难以进一步扩展。同时压缩两个维度虽有望实现更高内存节省，但直接组合会导致显著的精度损失（论文实验显示，在序列压缩基础上叠加70%通道压缩，精度损失达82.8%）。
- **整体目标**：设计一种动态二维（序列+通道）KV缓存压缩系统，实现低内存占用、高推理性能、低精度损失的长上下文LLM服务。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用KV缓存内元素重要性分布的非均匀性，以缓存段为粒度动态选择二维压缩策略，并设计压缩缓存管理系统来缓解细粒度稀疏性和管理开销。
- **关键技术细节**：
  1. **动态二维压缩（Dynamic Two-D Compression）**：
     - **两阶段压缩**：
       - 阶段一（通道压缩）：对每个KV向量进行SVD旋转后，独立选择每个向量的top-r重要元素（逐向量元素选择），而非全局统一掩码。
       - 阶段二（序列压缩）：在解码阶段，对于每个注意力层，从通道压缩后的KV缓存中动态选择top-k个token块（基于查询与块级键的注意力分数）。
     - **动态策略生成**：将KV缓存划分为多个段（segment），每个段独立生成压缩策略（包括通道压缩率、元素组大小、SVD旋转矩阵、序列块大小等）。划分依据是压缩损失阈值：每个新token加入当前段，累计压缩损失超过阈值或段长度达到上限时，终止当前段并生成新段的新策略。
  2. **压缩KV缓存管理（Compressed KV Cache Management）**：
     - **打包稀疏注意力（Packed Sparse Attention）**：将稀疏KV向量打包为密集格式（使用位置位图），利用未充分利用的GPU CUDA核心直接对打包向量执行点积，避免解包和转换开销，同时改善缓存局部性。
     - **异构双缓冲压缩（Heterogeneous Double Compression Buffering）**：GPU侧缓冲区临时存储新生成token的压缩KV向量；CPU侧缓冲区异步执行段划分、SVD计算、策略生成和重新压缩，通过切换机制替换临时压缩段，不阻塞解码阶段。
     - **增量策略生成**：通过增量更新协方差矩阵和压缩损失，快速判断段终止条件并生成新策略。

### 3. 实验设计：数据集、基准、对比方法

- **数据集与场景**：
  - **LongBench**：多任务长上下文理解基准（包含问答、摘要、代码生成等）。
  - **RULER**：64K上下文长度的长上下文推理基准（包含多键检索、多跳QA等）。
- **对比方法**：
  - Full baseline：无任何KV缓存压缩（使用FlashAttention或vLLM）。
  - Quest：仅序列压缩（动态token选择）。
  - 此外，论文还设计了消融实验，分析各优化组件（SVD旋转、逐向量选择、动态策略）的相对贡献。
- **评估指标**：准确率/分数、KV缓存内存占用、注意力延迟（分解为评分、选TopK、注意力计算）、解码延迟（TBT）、吞吐量、预填阶段压缩开销。
- **模型**：LLaMA-3.1-8B、Qwen3-4B、Ministral-3-8B，覆盖不同规模和架构（含GQA）。

### 4. 资源与算力

- **GPU型号**：NVIDIA H800（80GB HBM）用于性能评估；部分性能分析（如资源利用率剖析）使用A800。
- **GPU数量**：单张H800。
- **训练时长**：论文未涉及训练，仅推理优化。预填阶段压缩开销测量：64K token时开销7.6%，1M token时降至0.4%。
- **实现语言**：约5300行Python + 2800行CUDA。

### 5. 实验数量与充分性

- **实验组数**：包含：
  - 准确性评估：3个模型 × 2个基准（LongBench含12个子任务，RULER含8个子任务），共约60组结果。
  - 消融实验：1组（图9）展示逐步添加优化组件的精度变化。
  - 微基准测试：注意力延迟在不同上下文长度（64K–1M）、批次大小（1,4,8）、token选择率（6.25%,12.5%）下的分解。
  - 端到端性能：解码延迟和吞吐量随上下文长度变化（128K–1M），批次大小设为4。
  - 预填开销：5种上下文长度下的时间和占比。
  - 内存占用对比。
- **充分性与公平性**：实验覆盖了主要场景和多种模型，对比了最相关的方法（Quest），且消融实验清晰。所有方法使用相同浮点精度（BF16），默认配置相似（MosaicKV块大小8，Quest块大小16，均选择10% token）。但仍存在一定偏差：MosaicKV的通道压缩率固定25%，未与Quest的最优配置进行调优对比；Quest未使用通道压缩，硬件优势可能被放大。

### 6. 论文的主要结论与发现

- MosaicKV在H800 GPU上实现：
  - **注意力加速**：最高16倍于未压缩基线，2.4倍于Quest。
  - **解码延迟降低**：最高4.8倍于vLLM，1.8倍于Quest（1M上下文）。
  - **吞吐提升**：最高7.3倍于vLLM，2.2倍于Quest。
  - **内存减少**：KV缓存减少3倍，支持最大约1.44M token（vs. vLLM/Quest仅支持<512K）。
  - **精度损失**：LongBench平均损失0.86%，RULER平均损失1.86%（LLaMA-3.1-8B），总体平均1.76%。
- 动态二维压缩策略相较全局统一策略能有效保留重要元素，是维持高精度的关键。

### 7. 优点

- **方法创新**：首次在整个解码阶段同时实现序列和通道维度的动态压缩，通过逐向量选择和段级策略解决了组合压缩的精度问题。
- **性能优化**：利用闲置的GPU CUDA核心和CPU资源进行压缩管理及稀疏注意力计算，避免解包开销，将高压缩率转化为实际性能增益。
- **实现兼容性**：可集成FlashAttention、PagedAttention、RadixAttention等现有优化，支持GQA、MoE等架构，与量化正交。
- **实验全面**：覆盖多个模型、基准、上下文长度、批次大小，并提供细粒度延迟分解和消融分析。

### 8. 不足与局限

- **实验覆盖**：仅测试了最大1M上下文，未评估更极端长度（如10M token）的表现；仅使用单GPU，未探索多GPU分布式场景。
- **偏差风险**：默认通道压缩率固定为25%，未比较其他压缩率下的精度-性能折中；对比Quest时未进行调参公平性验证（例如Quest的块大小、选择率是否最优）。
- **应用限制**：当前仅优化解码阶段，预填阶段虽可通过分块预填加速但未深入实现；完全依赖CUDA核心进行打包注意力，若未来HBM带宽大幅提升，可能成为新的瓶颈。
- **资源与复现**：未提供开源代码或详细超参数设置，影响可复现性。
- **理论分析**：缺乏对压缩损失阈值选取的理论分析或敏感性实验，依赖经验设置。

（完）
