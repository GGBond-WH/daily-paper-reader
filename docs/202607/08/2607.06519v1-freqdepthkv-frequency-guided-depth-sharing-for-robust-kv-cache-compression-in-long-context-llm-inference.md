---
title: "FreqDepthKV: Frequency-Guided Depth Sharing for Robust KV Cache Compression in Long-Context LLM Inference"
title_zh: FreqDepthKV：频率引导的深度共享实现长上下文LLM推理中的鲁棒KV缓存压缩
authors: "Anna Córdoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesús Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06519v1"
tags: ["query:llm-kv-cache"]
score: 9.0
evidence: 频域引导的深度共享KV缓存压缩
tldr: 长上下文大语言模型推理受限于KV缓存的高内存和带宽开销，但激进压缩会丢失层特定证据。本文提出FreqDepthKV，通过频率引导的深度共享将相邻层KV状态分解为共享低频深度分量和稀疏高频残差，并结合轻量在线探头自适应分配注意力头到共享深度、残差深度或精确缓存模式。在长上下文问答、检索、摘要和代码生成任务中，该算法在显著减小缓存预算下保持任务精度，实现3.9倍有效压缩比，解码吞吐量达70.4 tokens/s，首 token 生成时间2.06秒，峰值KV内存6.2 GB。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06519v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1213, \"height\": 671, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06519v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1230, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06519v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1646, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06519v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1834, \"height\": 427, \"label\": \"Table\"}]"
motivation: 现有KV缓存压缩方法缺乏对层特定证据的保护，导致长上下文检索和多步推理任务性能下降。
method: 将相邻层KV状态分解为共享低频深度分量和稀疏高频残差，通过在线探头根据注意力头对重建敏感度的贡献动态分配缓存模式。
result: "32k预填窗口下，Exact Match 58.3，F1 63.0，ROUGE-L 32.5，pass@1 48.1，解码吞吐量70.4 tokens/s，TTFT 2.06s，峰值内存6.2 GB，有效压缩比3.9x。"
conclusion: FreqDepthKV通过频率引导的深度共享和自适应缓存分配，在保持长上下文推理准确性的同时大幅降低KV缓存开销，是一种无需重训的高效压缩方案。
---

## 摘要
长上下文LLM推理越来越受限于KV缓存的内存和带宽成本，然而激进的压缩可能移除检索和多步推理所需的层特定证据。我们提出了FreqDepthKV，一种推理时缓存压缩方法，将相邻层的KV状态分解为共享的低频深度分量和稀疏的高频残差。一个轻量级的在线探测根据注意力头对重建敏感注意力logits的贡献，将其分配到共享深度、残差深度或精确缓存模式，使得压缩策略能够适应提示结构而无需重新训练。在长上下文问答、针检索、摘要和代码生成基准测试中，FreqDepthKV在显著更小的缓存预算下保持了任务准确率。使用32k-token预填充窗口，FreqDepthKV达到了58.3 Exact Match、63.0 F1、32.5 ROUGE-L和48.1 pass@1，与完整KV接近，并优于先前的压缩缓存方法。它还提高了解码吞吐量至70.4 tokens/s，将TTFT降低至2.06秒，并将峰值KV内存降低至6.2 GB，实现了3.9倍的有效压缩比。

## Abstract
Long-context LLM inference is increasingly limited by the memory and bandwidth cost of KV caches, yet aggressive compression can remove the layer-specific evidence needed for retrieval and multi-step reasoning. We introduce FreqDepthKV, an inference-time cache compression method that factorizes adjacent-layer KV states into shared low-frequency depth components and sparse high-frequency residuals. A lightweight online probe assigns attention heads to shared-depth, residual-depth, or exact cache modes according to their contribution to reconstruction-sensitive attention logits, allowing the compression policy to adapt to prompt structure without retraining. Across long-context question answering, needle retrieval, summarization, and code generation benchmarks, FreqDepthKV preserves task accuracy under substantially smaller cache budgets. With a 32k-token prefill window, FreqDepthKV reaches 58.3 Exact Match, 63.0 F1, 32.5 ROUGE-L, and 48.1 pass@1, closely matching full KV while outperforming prior compressed-cache methods. It also improves decoding throughput to 70.4 tokens/s, reduces TTFT to 2.06 seconds, and lowers peak KV memory to 6.2 GB, achieving a 3.9x effective compression ratio.

---

## 论文详细总结（自动生成）

# 论文中文总结：FreqDepthKV

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：长上下文大语言模型（LLM）推理时，KV缓存的内存和带宽开销随上下文长度线性增长，成为瓶颈。现有KV缓存压缩方法（如令牌淘汰、量化、深度共享）虽然能减少开销，但激进的压缩往往移除层（layer）特定的关键证据，导致检索和多步推理任务性能下降。例如，统一深度共享（如MiniCache）假设相邻层缓存可安全替换，但在需要精确证据的场景下会破坏注意力logits。
- **动机**：需要一种既能大幅压缩KV缓存，又能保留对检索和推理至关重要的层特定高频残差信息的压缩方法。
- **整体含义**：本文提出**FreqDepthKV**，通过频率引导的深度共享，在压缩冗余的同时保护注意力敏感的稀疏证据，实现内存高效且准确的长上下文推理。

## 2. 方法论：核心思想与关键技术细节
- **核心思想**：将相邻层的KV状态分解为**共享的低频深度分量**（缓慢变化的层间成分）和**稀疏的高频残差**（保留检索敏感的层特定证据）。通过轻量级在线探头（probe）自适应地将每个注意力头分配到三种缓存模式：共享深度、残差深度、精确缓存，从而根据提示结构调整压缩策略。
- **关键技术细节**：
  - **深度-频率分解**：对每块相邻B层（默认B=4，首尾层B=2），沿深度轴堆叠KV缓存，应用固定DCT变换（正交基），分离低频共享分量和高频残差。
  - **在线路由与重建感知损失**：预填充期间，采样一组探测查询位置P（最近令牌、文档边界、高注意力熵行），对每个头计算三种模式下的重建注意力logit偏差：
    \[
    L_{b,h}(m) = \frac{1}{|P|} \sum_{t \in P} \left\| \frac{Q_{\ell,h,t} \tilde{K}_{(\ell,h)}^{(m)\top}}{\sqrt{d_h}} - \frac{Q_{\ell,h,t} K_{\ell,h}^{\top}}{\sqrt{d_h}} \right\|_2^2 + \lambda \Omega(m)
    \]
    其中 \(\tilde{K}^{(m)}\) 为模式m下的重建键缓存，\(\Omega(m)\) 为归一化内存成本。根据损失最小化选择模式。
  - **稀疏残差存储**：对于残差深度头，通过每个令牌移除高频残差后对logits的最大扰动评分，选前 \(r_{b,h}T\) 个令牌保留高频残差。
  - **压缩表示**：共享分量每块存储一次，残差分量仅存储索引令牌的高频系数，解码时广播共享系数并叠加稀疏残差。
  - **预算路由**：通过调整 \(\lambda\) 使峰值KV内存满足目标压缩比，一次路由后复用于整个解码过程，可定期刷新。

## 3. 实验设计
- **数据集与场景**：
  - QA与检索：LongBench、Needle-in-a-Haystack、L-Eval、NarrativeQA、Qasper。
  - 摘要：GovReport、MultiNews。
  - 代码生成：HumanEval、MBPP。
- **评测指标**：Exact Match (EM)、F1、ROUGE-L、pass@1，以及系统指标：解码吞吐量、首令牌时间（TTFT）、峰值KV内存、有效压缩比。
- **对比方法**：StreamingLLM、H2O、Scissorhands、SnapKV、PyramidKV、KVQuant、KIVI、MiniCache等基线方法。
- **固定设置**：所有方法使用相同的基础长上下文解码器（32k预填充窗口），仅在推理时应用，无重训。超参数在LongBench子集上调整后固定。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量或训练时长。
- 仅提到所有方法均为**推理时压缩**，无需模型重新训练，因此算力消耗主要在推理阶段。FreqDepthKV的在线探针和DCT变换开销很小（文中称“negligible prefill overhead”）。

## 5. 实验数量与充分性
- **实验数量**：
  - 主结果表1：在多个数据集上聚合报告EM、F1、ROUGE-L、pass@1及系统指标。
  - 消融实验表2：包含8种变体（无频率分解、无稀疏残差、无在线路由、无重建损失、无精确模式、仅共享深度、不同块大小等）。
  - 还提及在Needle-in-a-Haystack和代码任务上的额外分析（图中展示路由模式）。
- **充分性评估**：实验覆盖了长上下文QA、检索、摘要、代码生成四大类任务，对比了主流压缩方法。消融实验系统地分解了各组件贡献。但**局限性**：仅基于单一模型（未明确模型大小，可能是中等规模）和固定32k上下文，未在不同模型族或更长上下文中验证；此外，无多轮对话或复杂推理链的专门测试。

## 6. 主要结论与发现
- FreqDepthKV在所有任务指标上达到最佳压缩缓存准确率：EM 58.3、F1 63.0、ROUGE-L 32.5、pass@1 48.1，与完整KV缓存（Full KV）非常接近。
- 同时显著提升推理效率：解码吞吐量70.4 tokens/s，TTFT 2.06秒，峰值KV内存6.2 GB，有效压缩比3.9×。
- 相比于最强基线MiniCache，FreqDepthKV在更低内存下提高了所有任务指标（EM +1.7, F1 +2.0, ROUGE-L +1.2, pass@1 +2.5）。
- 频率分解和稀疏残差是鲁棒性的关键：去掉残差后EM降1.6，F1降2.1；仅共享深度则性能更差。
- 自适应路由和重建感知损失对于保留检索敏感证据至关重要。

## 7. 优点
- **创新性**：首次将层间深度冗余视为频率结构化信号，通过DCT分解和稀疏残差保留证据，设计精巧。
- **自适应性强**：在线探针根据提示结构动态选模式，无需重训，可适应不同任务。
- **兼容性好**：可正交结合令牌淘汰、量化等方法。
- **实验充分且公平**：与多个代表性基线在同一设定下对比，消融实验验证了各个组件。
- **系统收益显著**：在保持或接近完整KV准确率的同时，大幅降低内存并提高吞吐。

## 8. 不足与局限
- **路由固定性**：路由仅在预填充后确定，解码过程中不会更新，对于多轮生成或长对话中依赖变化的场景可能不够灵活（文中未来工作提及改进方向）。
- **模型泛化性未验证**：实验可能基于特定模型（如Llama系列），未测试更大参数规模或不同架构的模型。
- **上下文长度扩展性**：仅验证32k上下文，对128k或更长上下文的效果未知。
- **未与其他维度压缩联合优化**：虽提及兼容，但实验仅作为独立方法评估，未展示联合Token eviction或量化的综合效果。
- **计算开销的量化不足**：仅说明探针开销“可忽略”，但未给出具体FLOPs或时间占比。

（完）
