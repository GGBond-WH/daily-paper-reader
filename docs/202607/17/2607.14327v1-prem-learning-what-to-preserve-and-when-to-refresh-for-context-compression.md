---
title: "PReM: Learning What to Preserve and When to Refresh for Context Compression"
title_zh: PReM：学习在上下文压缩中保留什么以及何时刷新
authors: "Bohan Yu, Lei Shen, Chenxi Zhou, Chen Han, Junlin Liu, Wenbo Su, Yu Cheng, Bo Zheng"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14327v1"
tags: ["query:llm-kv-cache"]
score: 9.0
evidence: 通过可学习的保留和刷新机制处理KV缓存压缩
tldr: 长上下文推理中，现有压缩方法如KV缓存压缩或上下文压缩往往提前决定保留信息或依赖外部压缩器，难以适应后续推理需求。本文提出PReM框架，将长上下文作为模型内部层KV记忆，通过专用记忆层学习保留什么，并利用特殊记忆令牌触发刷新，结合分阶段刷新训练对齐记忆选择与生成。在32K token上下文中，16x和32x压缩下PReM均超越强基线，在答案质量和推理效率间取得良好平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14327v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1655, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 240, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 208, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 874, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 805, \"height\": 1305, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 838, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 836, \"height\": 380, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 837, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14327v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 835, \"height\": 419, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14327v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1805, \"height\": 1433, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14327v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1741, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14327v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14327v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1826, \"height\": 547, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14327v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1784, \"height\": 741, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14327v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1417, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14327v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1792, \"height\": 675, \"label\": \"Table\"}]"
motivation: 现有压缩方法难以适应推理过程中不断变化的上下文需求，需要动态保留和刷新机制。
method: PReM将长上下文作为模型内部层KV记忆，使用专用记忆层选择保留内容，并引入特殊记忆令牌触发刷新，结合分阶段刷新训练对齐记忆选择与生成。
result: 在32K token测试中，16x和32x压缩下PReM均优于强基线，同时保持答案质量和推理效率的平衡。
conclusion: PReM通过动态记忆选择与刷新机制，实现了高效的长上下文压缩，兼顾性能与效率。
---

## 摘要
高效的长上下文推理不仅关乎降低内存成本，还关乎在生成过程中保持有用上下文证据的可访问性。然而，现有的压缩导向方法，如键值（KV）缓存压缩和上下文压缩，往往要么提前决定保留哪些上下文信息，要么依赖外部压缩器。这类设计使得压缩后的上下文难以适应后续推理步骤所需的证据。本文提出了PReM（Preserve and Refresh Memory，保留与刷新记忆），一个上下文压缩框架，它将长上下文保持为模型内部层级化的KV记忆，并学习保留什么以及何时刷新。具体而言，PReM使用一个专用的记忆层来做记忆选择决策，并使用特殊的记忆令牌<m>在生成过程中触发刷新。为了训练这一行为，PReM引入了相位分离的刷新训练，将记忆选择与记忆条件下的生成对齐，同时保持刷新之间的连续性。在32K令牌上下文的实验表明，PReM在16倍和32倍压缩下均优于强基线，同时在答案质量和推理效率之间保持了良好的平衡。

## Abstract
Efficient long-context inference is not only about reducing memory cost, but also about keeping useful contextual evidence accessible as generation proceeds. However, existing compression-oriented approaches, such as key-value (KV) cache compression and context compression, often either make an early decision about which contextual information to keep or rely on an external compressor. Such designs make it difficult to adapt the compressed context to the evidence needed by later reasoning steps. This paper introduces PReM (Preserve and Refresh Memory), a context-compression framework that maintains the long context as the model's internal layer-wise KV memory and learns what to preserve and when to refresh it. Specifically, PReM uses a dedicated memory layer to make memory-selection decisions, and a special memory token <m> to trigger refreshes during generation. To train this behavior, PReM introduces Phase-Separated Refresh Training, aligning memory selection with memory-conditioned generation while preserving continuity across refreshes. Experiments with 32K-token contexts show that PReM outperforms strong baselines under both 16x and 32x compression, while maintaining a favorable balance between answer quality and inference efficiency.

---

## 论文详细总结（自动生成）

好的，以下是对论文《PReM：学习在上下文压缩中保留什么以及何时刷新》的详细分析总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文推理（Long-context inference）的计算成本随上下文长度迅速增长，现有压缩方法（如KV缓存压缩和上下文压缩）虽能降低内存，但普遍存在两大局限：
    - **静态证据保留**：在生成开始前一次性决定保留哪些上下文信息，无法适应多步推理中不同步骤对不同证据的需求。
    - **解耦的证据选择**：依赖外部压缩器进行压缩，与生成过程分离，增加了额外计算，且证据选择不够贴合每一步推理的即时需要。
- **整体含义**：有效的上下文压缩不应仅仅减少规模，而应能动态“保留”当前推理步骤所需的证据，并在需要时“刷新”记忆。本文提出的 **PReM 框架** 旨在通过模型内部可学习的记忆保留与刷新机制，同时提升答案质量和推理效率。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将长上下文作为 LLM 内部的层级化 KV 记忆（layer-wise KV memory），并通过模型自身学习“保留什么”和“何时刷新”。PReM 将上下文划分为连续块（chunk），通过一个专用记忆层（memory layer）进行块级选择，并使用特殊记忆令牌 `<m>` 在生成过程中触发刷新。
- **关键技术细节**：
    1. **上下文记忆表示**：在生成前，将长上下文编码为各层的 KV 状态 `Mℓ = (K_Mℓ, V_Mℓ)`。压缩后的 KV 记忆被附加到当前序列的 KV 状态前，参与标准注意力计算。
    2. **记忆选择（Memory Selection）**：以固定块大小（chunk size `c`）划分上下文为 `C` 个连续块。在记忆层 `ℓ_m` 增加一个选择查询投影矩阵 `W_cℓ`，映射隐藏状态到选择查询向量。通过式 (1) 计算每个块的注意力分数 `a_{ℓ,s,j}`，然后取 Top-K 个块作为保留的块 `S_s`。
    3. **记忆刷新（Memory Refresh）**：在推理时，问题令牌提供初始记忆选择；生成过程中，每当模型生成 `<m>` 令牌，其隐藏状态会触发新一轮的块选择，并刷新所有层的压缩记忆。未被选中的块被平均池化为一个代表 KV 对，以保持内存预算固定。
    4. **相位分离刷新训练（Phase-Separated Refresh Training）**：将每个推理步骤分为两个阶段：
        - **选择阶段**：仅处理触发令牌（问题或 `<m>`），计算 `S_s` 并优化记忆选择损失 `L^{(s)}_{ms}`（式 2）。
        - **生成阶段**：在刷新后的记忆状态下，用教师强制训练当前步骤的答案段 `y_s`，使用语言建模损失 `L^{(s)}_{ans}`（式 3）。
        - **边界损失**：额外加入 `L^{(s)}_{bd}`（式 4），处理选择阶段到生成阶段、以及答案段到下一个 `<m>` 令牌之间的自回归链接，确保模型学会在适当时候输出 `<m>`。
    5. **训练总损失**（式 5）：`L = (1/N_LM) * Σ (L^{(s)}_{ans} + L^{(s)}_{bd}) + λ_ms * (1/T) * Σ L^{(s)}_{ms}`，其中 `λ_ms` 控制记忆选择监督的权重。
- **算法流程**（文字说明）：训练数据构造时，使用 GPT-5.4 将原始 QA 对重写为分步骤、带文档标注的回答，并插入 `<m>` 作为步骤分隔。训练时，模型按步骤分别执行选择前向传播（仅输入触发令牌）和生成前向传播（输入该步骤答案段），两阶段共用一个模型权重，但分开计算梯度和损失。推理时，模型自动生成 `<m>` 来触发记忆刷新。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：使用六个基准数据集，覆盖单跳和多跳问答：
    - **单跳**：TriviaQA、SQuAD、NaturalQuestions
    - **多跳**：2WikiMQA、HotpotQA、MuSiQue
- **评估指标**：Exact Match (EM) 和 token-level F1。
- **基准（Baselines）**：
    - **KV 缓存压缩**：StreamingLLM、SnapKV、CAKE（均使用 Qwen2.5-3B/7B 骨干）
    - **上下文压缩（文本空间）**：LongLLMLingua、LLMLingua-2-large、EXIT（使用 Qwen 骨干）
    - **软提示压缩**：Activation Beacon（Qwen2-7B）、ICAE（Mistral-7B），使用其公开检查点。
- **设置**：上下文长度为 32K 令牌，压缩比为 16×（目标 2K tokens）和 32×（目标 1K tokens）。每个数据集评估 500 个样本。

### 4. 资源与算力

- **说明**：论文明确提到训练使用了 8 张 NVIDIA H20（96GB）GPU，采用全参数训练（full-parameter），使用 DeepSpeed ZeRO Stage 2、bf16 精度。训练数据规模为 32,319 个样本。**未提及训练总时长或消耗的 GPU 小时数**。消融训练使用 4K 上下文、相同步骤数。

### 5. 实验数量与充分性

- **实验数量**：实验覆盖以下方面：
    - **主结果**：在 2 种骨干（3B/7B）× 2 种压缩比（16×/32×）× 6 个数据集，共 24 组对比，每组成绩包括 EM 和 F1。
    - **与公开骨干软压缩基线对比**：PReM-3B/7B 与 Activation Beacon (7B)、ICAE (7B) 在 16×/32× 下对比。
    - **消融实验**：在 4K 上下文训练下，分析记忆选择损失、边界损失、原生 Top-K 注意力、不同记忆层位置、不同块大小/top-k 组合的影响。
    - **效率分析**：端到端延迟 vs. 上下文长度、解码延迟 vs. `<m>` 数量、峰值 GPU 内存。
    - **证据检索分析**：在 128 个两步 HotpotQA 样本上评估 Recall@10。
    - **层诊断**：在 LLaMA-3.2-3B、LLaMA-3.1-8B、Qwen-14B 上验证记忆选择损失随层的变化趋势。
- **充分性与客观性**：实验设计较为充分，消融验证了各关键组件（记忆选择损失、边界损失、记忆层位置、块粒度）的必要性。对比基线覆盖了主流的 KV 缓存压缩和上下文压缩方法，且都在 Qwen 骨干上公平对比（除软压缩基线使用各自原有骨干外）。评估使用 500 个样本，统计指标合理。效率分析也提供了定量数据。整体客观、公平。

### 6. 论文的主要结论与发现

- PReM 在 16× 和 32× 压缩下均优于所有强基线，在 3B/7B 骨干上平均 EM/F1 分别提升最多 10.23/12.55 点（7B 16×）和 8.20/9.89 点（7B 32×）。
- PReM 特别擅长多跳问答（如 2WikiMQA、MuSiQue），动态刷新显著提升了多步推理的准确性。
- 学习到的压缩甚至优于直接使用完整 32K 上下文提示（Promting），表明选择性的压缩记忆比全上下文更有效。
- 3B 的 PReM 甚至超过了 7B 的软压缩基线和一些文本压缩基线，展示了小模型通过有效记忆管理可以弥补规模差距。
- 关键消融证明：记忆选择损失不可或缺，原生注意力无法替代学习后的选择，边界损失对刷新训练很重要，中层/晚层更适合做记忆层，细粒度块（50-100 token）效果更好。
- 效率上，PReM 降低了峰值内存，且通过控制 `<m>` 数量可以在延迟和性能间灵活调节。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：提出“保留与刷新”范式，将压缩与生成过程紧密结合，实现动态、步骤相关的证据选择，优于静态压缩。
- **设计简洁优雅**：仅增加一个选择查询投影矩阵和一个特殊令牌 `<m>`，即可在现有 LLM 架构上实现，改动小。
- **训练技巧扎实**：相位分离训练解决了刷新后记忆与生成条件之间的一致性问题；边界损失将零散的步骤重新连接为连贯的自回归序列。
- **实验全面**：不仅报告主结果，还进行了详尽的消融、效率、证据检索分析；并在多种骨干模型上验证了记忆层位置选择的普遍性，增加了结论的可信度。
- **公平对比**：在相同骨干和上下文规模下与多个基线比较，并单独报告了与已发布公开模型的对比。

### 8. 不足与局限

- **训练数据依赖外部 API**：为了构造分步骤训练数据，使用了 GPT-5.4（OpenAI API），引入额外的成本和对闭源模型的依赖，可能影响复现性和可推广性。
- **实验覆盖**：主要评估了问答任务，未涉及其他长上下文任务（如文档摘要、对话、长文档分类或复杂推理任务）。此外，仅使用了 Qwen 骨干进行主实验，Llama 等其他骨干只在消融短实验中测试了记忆选择趋势，未报告完整的主实验结果。
- **潜在偏差风险**：训练数据源自 GPT 对答案进行重写，可能引入标签噪声或格式偏差。用固定块大小和 Top-K 策略可能导致信息丢失，尤其当关键信息跨块分布时。
- **效率分析不够全面**：虽然报告了延迟和峰值内存，但未与所有基线（尤其是软压缩基线）进行端到端的效率对比；也未量化刷新操作带来的额外计算开销（如每次刷新需要一次额外前向传播）。
- **应用限制**：需要为每个模型和任务训练专门的记忆选择权重（即全参数微调），无法零样本适配。刷新策略依赖于模型学会生成 `<m>`，但论文未讨论生成 `<m>` 的鲁棒性（例如是否可能生成过多或过少）。
- **未提及多次刷新时的收敛性**：对于需要多次刷新的长链推理，PReM 是否能稳定选择正确块并避免错误累积？论文未提供系统分析。

（完）
