---
title: "KAP: Bridging the Knowledge Selection-Runtime Consumption Gap in LLM Systems"
title_zh: KAP：弥合LLM系统中的知识选择-运行时消耗鸿沟
authors: "Shuo Wang, Fang Xi, Wenyuan Huang, Qing Wang, Junming Su"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24260v1"
tags: ["query:llm-kv-cache"]
score: 9.0
evidence: 提出弥合知识选择-运行时消耗差距，减少解码时的KV缓存占用和内存流量
tldr: "现代LLM系统依赖结构化知识选择（如排序证据、图拓扑）生成高价值先验，但推理时仅将提示序列化为扁平token，导致完整KV缓存被密集访问，造成长上下文场景下的延迟与吞吐瓶颈。为弥合这一“知识选择-运行时消耗”鸿沟，提出知识访问规划（KAP）框架，将结构化知识信号编译为运行时访问计划（IR），作为物理执行的一等公民，指导KV缓存的选择性访问而不改变模型或训练。实例化GraphSpec在4K-128K长上下文问答上，保持回答质量的同时，将提议阶段的KV访问降至源KV状态的5.5%，从根本上改变了长上下文生成的缩放轨迹。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24260v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1434, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24260v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1381, \"height\": 728, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24260v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1333, \"height\": 712, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24260v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 739, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24260v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 149, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24260v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 254, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24260v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1439, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24260v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1446, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24260v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 809, \"height\": 336, \"label\": \"Table\"}]"
motivation: 现有LLM系统在运行时忽略知识选择阶段产生的结构化先验，导致全提示KV缓存被均匀消耗，长上下文时内存流量和延迟剧增。
method: 提出知识访问规划（KAP）框架，通过运行时访问计划（IR）将结构化知识信号编译为物理执行指令，实现计划驱动的选择性KV访问。
result: "在4K-128K长上下文问答中，GraphSpec将提议阶段KV访问量降至源KV的5.5%，且回答质量与全上下文解码相当。"
conclusion: KAP通过解耦物理KV消耗与提示长度，为长上下文生成提供了可扩展的执行范式，显著降低了延迟与内存开销。
---

## 摘要
现代LLM系统日益依赖知识选择过程来生成高价值的结构化先验信息，如排序证据、图拓扑、多模态对齐和置信度信号。然而，LLM服务在本质上仍无视这种丰富结构：一旦这些信号被序列化为提示词，后端仅观察到平坦的令牌序列，迫使在解码期间密集且均匀地消耗完整的键值（KV）状态。我们将这种架构不匹配称为知识选择-运行时消耗（KSRC）鸿沟：更丰富的上下文会扩大全提示词的KV占用和解码时的内存流量，即使推理仅依赖上下文的一小部分，也会增加延迟并降低吞吐量。为弥合这一鸿沟，我们提出知识访问规划（KAP），这是一种范式转变的执行抽象，将结构化知识先验从被动的提示构造提示提升为第一类物理执行构件。KAP建立了一种通用中间表示（IR）——运行时访问计划——该计划编译结构化知识信号以指导物理KV访问，而不改变逻辑提示语义、模型权重或训练流程。通过此IR，KAP将LLM服务从基于令牌的上下文消耗转变为计划驱动的、知识感知的运行时消耗。我们通过GraphSpec实例化KAP，这是一种将结构化知识选择连接到LLM服务后端的编译器-执行器实现。我们推导了计划引导执行的正加速区域的相位边界模型。在4K-128K长上下文问答任务中，GraphSpec保持了与全上下文解码相当的答案质量，同时将物理KV消耗与提示长度解耦，在128K时将提议时间的KV访问减少到源KV状态的5.5%，并从根本上改变了长上下文生成的扩展轨迹。

## Abstract
Modern LLM systems increasingly rely on knowledge-selection processes that produce high-value structured priors, such as ranked evidence, graph topology, multimodal alignment, and confidence signals. Yet LLM serving remains fundamentally oblivious to this rich structure: once such signals are serialized into a prompt, the backend observes only a flat token sequence, forcing dense and uniform consumption of the full key-value (KV) state during decoding. We term this architectural mismatch the Knowledge Selection-Runtime Consumption (KSRC) gap: richer contexts enlarge the full-prompt KV footprint and decode-time memory traffic, increasing latency and degrading throughput even when reasoning depends on only a small fraction of the context. To bridge the gap, we propose Knowledge Access Planning (KAP), a paradigm-shifting execution abstraction that elevates structured knowledge priors from passive prompt-construction hints into first-class physical execution artifacts. KAP establishes a universal intermediate representation (IR)-the runtime access plan-which compiles structured knowledge signals to govern physical KV access without altering logical prompt semantics, model weights, or training procedures. Through this IR, KAP shifts LLM serving from token-aware context consumption to plan-driven, knowledge-aware runtime consumption. We instantiate KAP with GraphSpec, a compiler-executor realization connecting structured knowledge selection to an LLM serving backend. We derive a phase-boundary model for the positive-speedup regime of plan-guided execution. Across 4K-128K long-context QA workloads, GraphSpec maintains answer quality comparable to full-context decoding while decoupling physical KV consumption from prompt length, reducing proposal-time KV access to 5.5% of source KV state at 128K, and fundamentally shifting the scaling trajectory of long-context generation.

---

## 论文详细总结（自动生成）

## 论文总结：KAP：弥合LLM系统中的知识选择-运行时消耗鸿沟

### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：现代LLM系统依赖知识选择前端（如检索增强生成、图RAG）生成结构化知识先验（排序证据、图拓扑、置信度信号等），但LLM服务后端仅接收序列化的扁平提示词，忽略这些结构。这导致在解码时，后端必须密集地、均匀地消耗完整的KV缓存，即使推理仅依赖上下文的一小部分。这种架构不匹配被定义为**知识选择-运行时消耗（KSRC）鸿沟**。
- **整体含义**：随着上下文窗口扩展到百万令牌，如果物理消耗仍随序列化长度线性增长，LLM服务的瓶颈将越来越严重。作者主张需要知识选择-执行协同设计，使物理成本随上下文长度**次线性**扩展。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法
- **核心思想**：提出**知识访问规划（KAP）**框架，将结构化知识先验从被动的提示构造提示提升为第一类物理执行构件。关键在于引入**运行时访问计划（Runtime Access Plan）**——一种通用中间表示（IR），将结构化知识信号编译为物理KV访问策略，而不改变逻辑提示语义、模型权重或训练流程。
- **关键技术细节**：
  - **IR定义**：运行时访问计划 A = (U, M, pos, ρ_access, ρ_verify)，其中U为选定的知识单元，M为运行时对象映射，pos为逻辑位置，ρ_access和ρ_verify分别为访问和验证策略。
  - **KAP编译器-执行器架构**：编译器将知识先验编译为运行时访问计划；执行器解释计划进行计划引导的选定KV访问，并通过验证（如全上下文验证）确保输出正确性。
  - **GraphSpec实例化**：以前端Graph-RAG和后端VLLM实现KAP。编译器将图派生先验（证据跨度）映射到VLLM的分页KV页面，构建紧凑的选定KV视图（保留逻辑位置），并生成访问计划；执行器采用**选定KV自我推测解码**：基础模型在选定KV视图上生成候选令牌（提案路径），然后通过全上下文验证接受或拒绝令牌，只提交被接受的令牌。
  - **相位边界模型（Phase-boundary model）**：导出正加速条件：
    - 通用公式：`(h - s)KV > W_prop - hW_main`，其中h为接受率，s为保留KV比例，KV为读取完整KV成本，W为权重成本。
    - 对于GraphSpec（自推测，W_prop = W_main）：简化为`h > s`，且理想盈亏平衡KV规模`KV_crit ≈ (1-h)W_main / (h-s)`。该模型将知识选择质量转化为执行效率的定量标准。

### 3. 实验设计：数据集、场景、基准、对比方法
- **数据集**：**SPIQA**（多模态QA基准，基于科学论文中的图表和文本）。作者保留答案和核心证据，通过添加查询特定的硬负例来构造不同上下文长度（4K、8K、16K、32K、64K、128K）。
- **场景**：长上下文知识增强LLM服务。
- **基准方法**：
  - **全上下文基线**：标准VLLM解码，使用完整KV状态。
  - **GraphSpec**：提出的方法（前端Graph-RAG，后端VLLM，推测长度k=6）。
  - **Physical concat**：将GraphSpec选中的证据跨度作为提示压缩器，形成短提示（不保留完整逻辑上下文）。
  - **Random concat**：随机选择相同数量令牌形成短提示（无引导）。
  - **Local window**：保留序列化上下文的固定窗口（位置启发式）。
- **评估指标**：
  - 效率：解码吞吐量（TPS）、首令牌时间（TTFT）、保留KV比例s、接受率h、h-s。
  - 质量：LLM相似度评分（四个维度加权，0-1），通过率（分数≥0.5的样本比例）。通过人为审计验证阈值可靠性（95%一致率）。

### 4. 资源与算力
- **模型**：Qwen3-VL-32B-Instruct（约32B参数）。
- **硬件**：4个NVIDIA A800 80GB GPU，采用张量并行4。
- **精度**：bfloat16。
- **服务引擎**：扩展VLLM v0.16.0。
- **训练**：未进行训练；所有实验均为推理服务评估。论文未提及训练时长或预训练资源。

### 5. 实验数量与充分性
- **实验组数**：围绕四个研究问题（RQ1-RQ4）展开，共约5个主要表格（Table 2-6），涵盖：上下文长度扫描（6档）、质量对比、消融实验、相位边界验证。
- **消融实验**：RQ3明确消融执行策略（Physical concat、Random concat、Local window），证明运行时访问计划（而非仅证据选择）是关键。但缺乏与其他KV缓存压缩方法（如H2O、SnapKV、Quest等）的比较。
- **充分性与公平性**：优点包括：上下文长度控制良好（固定核心证据，添加硬负例构造长度，而非不同数据集）；所有方法使用相同模型、解码配置、软件栈；质量指标经过人工审计验证阈值；相位边界预测与实验趋势一致。**不足**：仅评估一个数据集（SPIQA）、一个模型（Qwen3-VL-32B）、一个前端（Graph-RAG）和后端（VLLM），泛化性有限。未进行不同模型大小或架构的对比。所有实验在控制环境中进行，可能不完全反映真实服务中的调度/争用开销。

### 6. 论文的主要结论与发现
- **RQ1**：GraphSpec解耦物理KV消耗与序列化上下文长度。在128K时，提案可见KV仅占源KV的5.5%（s=0.055），解码吞吐量提升1.19×（全上下文41.10 TPS → 48.80 TPS）。TTFT基本不变（因为保留了完整逻辑提示的预填充）。
- **RQ2**：GraphSpec在所有上下文长度下保持与全上下文解码相当的答案质量（通过率差异≤2个百分点，平均相似度几乎相等）。
- **RQ3**：消融实验证实，仅将相同证据作为短提示（Physical concat）导致质量显著下降（通过率73.9% vs GraphSpec 87.9%），说明运行时访问计划是弥合KSRC鸿沟的关键机制。
- **RQ4**：观测到的吞吐量加速趋势与相位边界模型一致：随着上下文变长，s下降、h维持高位，h-s增大，加速比从1.08×升至1.19×。
- **核心结论**：通过运行时访问计划将知识选择前端的结构化先验转化为可执行消耗策略，可以显著降低LLM服务的物理KV消耗和延迟，同时不牺牲答案质量。KAP为知识选择-执行协同设计提供了通用执行基座，使物理成本随上下文长度**次线性**扩展成为可能。

### 7. 优点
- **问题识别新颖**：明确识别KSRC鸿沟这一此前未被充分探索的架构问题。
- **范式创新**：提出KAP抽象，以运行时访问计划作为通用IR，将知识选择与运行时消耗解耦，不依赖特定模型、模态或服务框架。
- **理论与实践结合**：推导相位边界模型，提供正加速区域的分析准则，将知识质量转化为执行效率的定量条件。
- **实例化完整**：GraphSpec作为可部署参考实现，连接Graph-RAG和VLLM，展示了从编译器到执行器的完整流程。
- **实验设计严谨**：核心证据固定、硬负例构建不同长度，有效隔离上下文长度影响；质量指标经人工验证。
- **消融实验有力**：通过Physical concat等对比，清晰证明运行时访问计划（而非仅证据选择）是效率-质量平衡的关键。

### 8. 不足与局限
- **实例化局限**：仅评估一个具体实例GraphSpec（Graph-RAG + VLLM + Qwen3-VL-32B），不能代表所有KAP实现。其他前端（如多模态对齐、智能体记忆）或后端（其他缓存机制、注意力加速器）未验证。
- **数据集单一**：仅使用SPIQA一个数据集（多模态科学QA）。缺少其他长上下文任务（如文档问答、多文档摘要）的评估。
- **未与其他KV缓存压缩方法比较**：实验未包含如H2O、SnapKV、Quest等主流KV缓存压缩方法，削弱了对KAP独特优势的论证。作者在相关工作中提及这些方法，但未在实验中对比。
- **相位边界模型简化**：模型假设内存带宽受限、忽略元数据开销、调度开销和内核启动开销，现实加速可能低于理想值。论文也指出TTFT改善有限，因为保留完整逻辑提示。
- **质量评估局限**：LLM作为评委可能带有偏差；人为审计仅100个样本，可能不足以代表整体。通过率0.5阈值固定，但不同任务可能不同。
- **可扩展性未知**：当前实验使用4个GPU、32B模型，更大模型（70B+）或更长上下文（1M+）的表现未探索。
- **知识选择前端开销**：实验中Graph-RAG证据和先验在计时前已冻结，但实际应用中前端检索/构建图也有时间成本，本研究未将其纳入端到端延迟评估（TTFT测量从请求提交开始，但排除了前端步骤）。论文明确标注“frozen before benchmarking”。

（完）
