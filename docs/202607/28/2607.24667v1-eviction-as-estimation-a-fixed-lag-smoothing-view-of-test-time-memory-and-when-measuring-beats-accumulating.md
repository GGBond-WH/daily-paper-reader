---
title: "Eviction as Estimation: A Fixed-Lag Smoothing View of Test-Time Memory, and When Measuring Beats Accumulating"
title_zh: 作为估计的驱逐：测试时记忆的固定滞后平滑视角，以及测量何时胜过累积
authors: "Maruthi Vemula, Neeraj Praneeth Gajula"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24667v1"
tags: ["query:llm-kv-cache"]
score: 9.0
evidence: 基于固定滞后平滑的LLM KV缓存驱逐策略
tldr: 语言模型有界工作记忆需决定驱逐策略，现有方法在项到达时立即决策（如StreamingLLM、H2O）或基于未来猜测（如SnapKV）。本文将其重述为隐藏信号估计问题，提出固定滞后平滑策略RMM，等待有限步后根据模型对近未来预测的注意力（demonstrated utility）驱逐，是H2O的严格泛化。控制实验中，当重用内生且时间分离时，RMM显著优于累积注意力；但在独立基准（如KVPress）中，RMM与H2O相当，弱于SnapKV，原因在于自然文本模型对大多数token正确，使测量几乎等同于累积。贡献在于提供了新框架及“何时测量优于累积”的诚实映射，而非追求新最优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24667v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1359, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24667v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 792, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24667v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1207, \"height\": 713, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24667v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24667v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1123, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24667v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1225, \"height\": 135, \"label\": \"Table\"}]"
motivation: 现有驱逐策略在项到达时立即决策，缺乏利用近未来信息的能力，本文旨在通过固定滞后平滑填补这一空白。
method: 提出固定滞后平滑策略RMM，等待有限步后根据模型对近未来预测的注意力（demonstrated utility）驱逐，是H2O的严格泛化。
result: 控制实验中RMM优于累积注意力，但在标准基准上与H2O相当，弱于SnapKV，因自然文本模型多数正确。
conclusion: "提供了新框架及'何时测量优于累积'的诚实映射，强调测量仅在重用内生且时间分离时有效。"
---

## 摘要
一个具有有限工作记忆的语言模型必须反复决定保留哪些存储项。每一种已部署的方法都在项目到达时做出决定，依据过去（StreamingLLM、H2O）或对未来（SnapKV）的猜测。我们将这一选择重新表述为关于隐藏信号（即项目是否会被重用）的估计问题，并将现有方法置于一个轴上，即提交滞后$H$：在线过滤器和学习型预测器在$H=0$时提交，而Belady的离线最优则在整个未来已知的情况下成立。介于两者之间的缺失机制，即固定滞后平滑，等待有限步数，观察正确的近期预测关注了哪些项目，然后才提交。这种测量，即已证效用，将Belady的不可观测的未来请求转化为我们从模型自身读取的内容。我们将其实例化为一种无需训练的策略RMM，它是H2O的严格泛化，当测量均匀时恰好退化为H2O。在重用是内生的且时间分离的受控设置中，已证效用识别已用记忆的能力远优于累积注意力，且小的有界记忆表现得像大得多的记忆。但在独立的第三方基准上，在NVIDIA的KVPress框架内与其自身的SnapKV、H2O和StreamingLLM实现一起运行时，优势基本消失：对于单轮问答，RMM与H2O相当，在流式多轮设置中则输给H2O和SnapKV。原因很简单：在自然文本上，模型对大多数词元是正确的，因此按正确性加权注意力几乎不改变它，并且除非重用是尖锐且内生的（标准基准未涉及），已证效用会坍缩为累积注意力。我们的贡献在于框架以及关于测量何时胜过累积的诚实图谱，而非新的最先进技术。

## Abstract
A language model with a bounded working memory must repeatedly decide which stored items to keep. Every deployed method decides the moment an item arrives, from the past (StreamingLLM, H2O) or from a guess about the future (SnapKV). We recast the choice as an estimation problem on a hidden signal, whether an item will be reused, placing existing methods on one axis, the commit lag $H$: online filters and learned predictors commit at $H=0$, while Belady's offline optimum sits where the whole future is known. The missing regime in between, fixed-lag smoothing, waits a bounded number of steps, observes which items a correct near-future prediction attended to, and only then commits. This measurement, demonstrated utility, turns Belady's unobservable future request into something we read off the model itself. We instantiate it as a training-free policy, RMM, a strict generalization of H2O that reduces to it exactly when the measurement is uniform. In controlled settings where reuse is endogenous and separated in time, demonstrated utility identifies used memory far better than accumulated attention, and a small bounded memory behaves like a much larger one. But on independent third-party benchmarks, run inside NVIDIA's KVPress harness against its own SnapKV, H2O, and StreamingLLM implementations, the advantage mostly disappears: RMM is on par with H2O for single-turn question answering and loses to both H2O and SnapKV in a streaming multi-turn setting. The cause is simple: on natural text the model is correct about most tokens, so weighting attention by correctness barely changes it, and demonstrated utility collapses onto accumulated attention unless reuse is sharp and endogenous, which standard benchmarks do not exercise. Our contribution is the framework and an honest map of when measuring beats accumulating, not a new state of the art.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：语言模型在有界工作记忆（如 KV 缓存或循环状态）中，每步都需要决定保留哪些项、驱逐哪些项。现有方法要么根据过去的统计（如 StreamingLLM 的邻近性、H2O 的累积注意力），要么根据对未来的预测（如 SnapKV、PyramidKV）在项到达时立即决策。然而，一个项的真实效用只有在其被未来的查询实际使用时才显现，因此立即决策本质上是猜测。
- **研究动机**：作者将驱逐问题重新表述为隐藏信号（项是否会被重用）的估计问题，认为已有方法都处于“提交滞后 H = 0”的极端，而经典的最优离线算法 Belady 则是 H → ∞。两者之间存在一个未被探索的中间地带：**固定滞后平滑**（fixed-lag smoothing），即等待有限步数，观测并测量哪些项被正确的近未来预测实际使用了，然后才决定驱逐。这可以借鉴定位理论中的平滑思想。
- **整体含义**：论文旨在提供一个统一框架，并诚实评估“测量近未来效用”是否优于“累积历史注意力”。控制实验表明在特定条件下（重用内生且时间分离）测量远优于累积，但在独立基准上优势消失，从而揭示了“测量何时胜过累积”的条件边界。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将驱逐决策视为一个固定滞后平滑问题。引入一个可调参数**提交滞后 H**：H = 0 对应在线滤波器/预测器（如 H2O、SnapKV）；H → ∞ 对应 Belady 的离线最优；中间 H = lag 对应本文提出的平滑策略 RMM，它等待 H 步，利用这期间模型自身的正确预测产生的注意力来评估每个缓存项的效用。
- **关键技术细节**：
  - **Demonstrated Utility（已证效用）**：对于缓存中的项 k，其已证效用定义为在滞后窗口 (t_k, t_k+H] 内，来自模型正确预测的位置的注意力之和：`DU_H(k) = Σ_{t'∈(tk, tk+H]} 𝕀[prediction at t' correct] · a(t' → k)`。其中正确性通过 teacher forcing 检查（模型预测与真实下一个 token 一致）。这个信号是自监督的，无需额外标签。
  - **RMM 策略**：每个新到达的项先进入一个大小为 H 的临时缓冲区（provisional buffer），尚未提交。在缓冲区内仍可被读取。当项老化 H 步后，计算其已证效用；如果它高于已提交缓存中最弱项的已证效用，则提交（保留）它，否则驱逐。已提交缓存保持 K 个项，缓冲区保持 H 个，总内存为 K+H。
  - **与 H2O 的关系**：H2O 的分数是累积注意力 `Σ_{t'} a(t' → k)`。已证效用等同于累积注意力乘以每个位置预测正确性的权重。当所有预测都被视为正确（uniform weight）时，RMM 完全退化为 H2O。因此 RMM 是 H2O 的严格泛化，理论上不差于 H2O。
  - **小模型分析**：给出了一个简化模型（有 N 个项，其中 U 个有用、K 个缓存容量、H = 滞后），证明了平滑器可达到 Belady 最优（召回率 min(1, K/U)），而在线滤波器召回率仅约 K/N，学习型预测器只有准确率 p → 1 时才追上平滑器。这预测了控制实验中的排序。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **控制实验（机制分析）**：
  - 多跳推理任务：在 Qwen2.5-7B 上让模型自己生成思维链，回答需要链接两个事实的多跳问题（14 个干扰项）。测量已证效用与累积注意力/最近性对关键事实的识别能力。
  - 流式代理任务：构建一个多步流式场景，事实不断流入，记忆限制为 K=40，其中部分事实在出现后不久被重用（带显式提醒），其余永不重用。测量不同策略在长轨迹中对重用的保留率。
- **独立基准（第三方框架内）**：
  - **单轮预填充压缩**：LongBench 中的 HotpotQA（F1）和 TREC（准确率），两种压缩级别（保留 50% 和 25%）。
  - **流式多轮**：LoCoMo 上的多轮对话，每轮前将整个历史压缩到 1800 token 预算，评估后几轮（in-regime turns）的 F1 分数。
  - **基准对比方法**：SnapKV、H2O、StreamingLLM，均使用 NVIDIA KVPress 框架中的官方实现。RMM 作为评分器嵌入该框架，与 H2O 的唯一区别是正确性权重。
- **模型**：全程使用冻结的 Qwen2.5-7B，eager attention 模式。

### 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量、训练时长。仅提到“冻结的 Qwen2.5-7B 使用 eager attention”，且由于读取注意力（RMM 和 H2O 均需要），可运行的上下文长度被限制在约 8000 token 以内，因此无法测试更长的 SCBench 基准。未提供详细的硬件规格或运行时间。研究者推测实验可能在单张高端 GPU（如 NVIDIA A100 或 H100）上完成，但无明确记载。

### 5. 实验数量与充分性

- **控制实验**：两个任务（多跳推理、流式代理），每组提供定量结果（如图 2 的召回率曲线、多跳推理中正确识别概率约 0.8 vs 0.1）。设计旨在隔离机制，实验数量有限但针对性强。
- **独立基准**：LongBench 任务两个（HotpotQA、TREC），每个两种预算，n=40 示例；LoCoMo 流式，n=8 对话（仅后几轮，共约 8×3=24 个样本点？论文只说 n=8 个对话，表 3 显示单值）。还提及 RULER 和 SCBench 但未使用（因上下文长度限制）。总体实验数量偏少，但作者诚实声明这是第三方框架内的“负载承载”比较，用于回答问题：“RMM 能否在未调优的数据上胜出？”答案是“不能”，因此实验在支持其结论方面是充分的。
- **公平性**：RMM 实现在 KVPress 中只是修改 H2O 的评分函数，且验证了均匀权重下完全复制 H2O 结果。对比方法均使用同一框架的官方实现，模型冻结、预算匹配，因此实验条件是公平的。作者也承认自己构建的控制任务可能夸大效果，但独立基准使用了他人代码与数据，减轻了偏差。

### 6. 论文的主要结论与发现

- **正面发现**：在所构建的受控设置中（重用内生且时间分离），已证效用（demonstrated utility）识别已用记忆的效果远超累积注意力和最近性；有界记忆在 RMM 下表现接近无界 oracle。这些效果验证了固定滞后平滑机制的有效性。
- **负面发现**：在第三方独立基准（LongBench、LoCoMo）上，RMM 与 H2O 在单轮预填充中统计上不可区分，在流式多轮中实际**输给** H2O 和 SnapKV。原因在于：自然文本中模型预测正确率很高（约 85%~90%），因此正确性权重接近均匀，已证效用几乎坍缩为累积注意力；只有在正确性选择性强、局部集中时两者才分离，而标准基准不体现这种模式。
- **核心结论**：论文的贡献在于**框架**和**关于“测量何时胜过累积”的诚实映射**，而非提出一个新 SOTA 方法。RMM 虽然是 H2O 的严格泛化，但在实际部署场景中并未超越现有方法。作者明确指出“the useful contribution is the framework together with a clear account of when the idea helps.”

### 7. 优点

- **框架创新**：将驱逐问题与估计理论中的滤波、预测、平滑三个经典立场联系起来，引入“提交滞后 H”作为统一轴，连接了在线启发式、学习预测器和离线最优。这个视角清晰且有启发性。
- **Belady 桥梁**：提出了“已证效用”作为 Belady 中未来请求的无标签可观测替代，成功将最优缓存思想引入神经网络记忆。
- **RMM 理论性质**：是 H2O 的严格泛化，保证不差于 H2O（当正确性信息无用时退化）。论文从小模型分析到控制实验再到独立基准，逻辑链条完整。
- **诚实性**：作者没有夸大结果，而是详细报告了负面发现并解释原因，为社区提供了有价值的“负面但有信息量”的结论，有助于指导未来研究方向。
- **实验设计**：区分了机制验证（控制实验）和负载比较（独立基准），分别使用自制任务和第三方框架，确保了结论的可靠性。

### 8. 不足与局限

- **实验规模与统计功效**：独立基准样本量较小（如 LoCoMo 仅 8 个对话），可能影响结论的统计稳定性。作者未进行多次运行或置信区间报告（仅给出单点值）。
- **可扩展性**：RMM 需要 eager attention 来获取所有注意力权重，这限制了上下文长度（论文提到约 8000 token），无法在更长上下文基准（如 SCBench、RULER）上测试。作者承认这一点且未尝试优化（例如使用 SnapKV 式的小窗口重计算）。
- **部署可行性**：RMM 评估时需要 teacher forcing 检查正确性，这在真实推理中不可用（未来 token 未知）。作者提到可用置信度分数或验证器作为代理，但未测试，因此实际部署方案不明确。
- **局限性区域**：论文明确指出在自然文本上，正确性权重几乎为常数，因此 RMM 与 H2O 无异。这意味着该方法真正受益的场景需要额外的假设（重用尖锐、内生、局部集中），这类场景缺乏标准 benchmark。作者认为这是一个未填写的基准缺口，而非 RMM 本身的失败。
- **消融实验不足**：没有探索不同 H 值的影响，没有对比 RMM 与其他 smooth-like 方法（如延迟决策的其他变体）。仅对比了 H=0 的方法（过滤器/预测器）和 H→∞ 的 oracle。

（完）
